import { Leaderboard } from "@/components/leaderboard/leaderboard";

export const metadata = {
  title: "Leaderboard",
  description: "See the top learners in the Superteam Academy community",
};

export default function LeaderboardPage() {
  return <Leaderboard />;
}
