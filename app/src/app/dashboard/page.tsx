export default function DashboardPage() {
  return (
    <div className="mx-auto max-w-6xl px-4 py-8">
      <h1 className="font-display text-text-primary text-2xl font-semibold">Dashboard</h1>
      <p className="text-text-secondary mt-2 text-sm">XP, streak, and courses will appear here (Phase 4).</p>
    </div>
  );
}
