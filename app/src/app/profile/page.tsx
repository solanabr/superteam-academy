import { ProfilePage } from "@/components/profile/profile-page";

export const metadata = {
  title: "Profile",
  description: "View your learning profile, credentials, and achievements",
};

export default function Profile() {
  return <ProfilePage />;
}
