/**
 * Anchor program IDL — loaded from the real compiled IDL JSON.
 * Program: 64XGGSc32TUX7rxge5u4Qsv55RQN5ybSwS4B1eksWTxy (devnet)
 *
 * To update after a program upgrade:
 *   cd ../onchain-academy && anchor build
 *   cp target/idl/onchain_academy.json ../app/src/lib/onchain_academy.json
 */

// eslint-disable-next-line @typescript-eslint/no-require-imports
export const IDL = require("./onchain_academy.json");
