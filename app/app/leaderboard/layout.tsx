/**
 * Leaderboard layout — passthrough. Navbar shown/hidden in NavbarWrapper by login state.
 */
export default function LeaderboardLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
