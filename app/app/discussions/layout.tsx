/**
 * Discussions layout — passthrough. Navbar shown/hidden in NavbarWrapper by login state.
 */
export default function DiscussionsLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
