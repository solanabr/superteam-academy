export const metadata = {
  title: 'Course catalog | Superteam Brazil LMS',
  description: 'Browse Solana courses. Filter by difficulty, topic, and duration. Track progress with your wallet.',
};

export default function CoursesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
