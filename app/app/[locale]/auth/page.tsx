/**
 * @fileoverview Unified authentication page.
 * Serves as the main entry point for login and signup flows.
 */
import { AuthView } from "@/components/auth/AuthView";

/**
 * Renders the primary authentication view.
 */
export default function AuthPage() {
	return <AuthView />;
}
