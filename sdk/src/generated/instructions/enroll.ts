/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ONCHAIN_ACADEMY_PROGRAM_ADDRESS } from "../programs";

export const ENROLL_DISCRIMINATOR = new Uint8Array([
  58, 12, 36, 3, 142, 28, 1, 43,
]);

export function getEnrollDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ENROLL_DISCRIMINATOR);
}

export type EnrollInstruction<
  TProgram extends string = typeof ONCHAIN_ACADEMY_PROGRAM_ADDRESS,
  TAccountCourse extends string | AccountMeta<string> = string,
  TAccountEnrollment extends string | AccountMeta<string> = string,
  TAccountLearner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCourse extends string
        ? WritableAccount<TAccountCourse>
        : TAccountCourse,
      TAccountEnrollment extends string
        ? WritableAccount<TAccountEnrollment>
        : TAccountEnrollment,
      TAccountLearner extends string
        ? WritableSignerAccount<TAccountLearner> &
            AccountSignerMeta<TAccountLearner>
        : TAccountLearner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type EnrollInstructionData = {
  discriminator: ReadonlyUint8Array;
  courseId: string;
};

export type EnrollInstructionDataArgs = { courseId: string };

export function getEnrollInstructionDataEncoder(): Encoder<EnrollInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["courseId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: ENROLL_DISCRIMINATOR }),
  );
}

export function getEnrollInstructionDataDecoder(): Decoder<EnrollInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["courseId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getEnrollInstructionDataCodec(): Codec<
  EnrollInstructionDataArgs,
  EnrollInstructionData
> {
  return combineCodec(
    getEnrollInstructionDataEncoder(),
    getEnrollInstructionDataDecoder(),
  );
}

export type EnrollAsyncInput<
  TAccountCourse extends string = string,
  TAccountEnrollment extends string = string,
  TAccountLearner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  course?: Address<TAccountCourse>;
  enrollment?: Address<TAccountEnrollment>;
  learner: TransactionSigner<TAccountLearner>;
  systemProgram?: Address<TAccountSystemProgram>;
  courseId: EnrollInstructionDataArgs["courseId"];
};

export async function getEnrollInstructionAsync<
  TAccountCourse extends string,
  TAccountEnrollment extends string,
  TAccountLearner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ONCHAIN_ACADEMY_PROGRAM_ADDRESS,
>(
  input: EnrollAsyncInput<
    TAccountCourse,
    TAccountEnrollment,
    TAccountLearner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  EnrollInstruction<
    TProgramAddress,
    TAccountCourse,
    TAccountEnrollment,
    TAccountLearner,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ONCHAIN_ACADEMY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    course: { value: input.course ?? null, isWritable: true },
    enrollment: { value: input.enrollment ?? null, isWritable: true },
    learner: { value: input.learner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.course.value) {
    accounts.course.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 117, 114, 115, 101])),
        getUtf8Encoder().encode(
          getNonNullResolvedInstructionInput("courseId", args.courseId),
        ),
      ],
    });
  }
  if (!accounts.enrollment.value) {
    accounts.enrollment.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([101, 110, 114, 111, 108, 108, 109, 101, 110, 116]),
        ),
        getUtf8Encoder().encode(
          getNonNullResolvedInstructionInput("courseId", args.courseId),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "learner",
            accounts.learner.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("course", accounts.course),
      getAccountMeta("enrollment", accounts.enrollment),
      getAccountMeta("learner", accounts.learner),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getEnrollInstructionDataEncoder().encode(
      args as EnrollInstructionDataArgs,
    ),
    programAddress,
  } as EnrollInstruction<
    TProgramAddress,
    TAccountCourse,
    TAccountEnrollment,
    TAccountLearner,
    TAccountSystemProgram
  >);
}

export type EnrollInput<
  TAccountCourse extends string = string,
  TAccountEnrollment extends string = string,
  TAccountLearner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  course: Address<TAccountCourse>;
  enrollment: Address<TAccountEnrollment>;
  learner: TransactionSigner<TAccountLearner>;
  systemProgram?: Address<TAccountSystemProgram>;
  courseId: EnrollInstructionDataArgs["courseId"];
};

export function getEnrollInstruction<
  TAccountCourse extends string,
  TAccountEnrollment extends string,
  TAccountLearner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ONCHAIN_ACADEMY_PROGRAM_ADDRESS,
>(
  input: EnrollInput<
    TAccountCourse,
    TAccountEnrollment,
    TAccountLearner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): EnrollInstruction<
  TProgramAddress,
  TAccountCourse,
  TAccountEnrollment,
  TAccountLearner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ONCHAIN_ACADEMY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    course: { value: input.course ?? null, isWritable: true },
    enrollment: { value: input.enrollment ?? null, isWritable: true },
    learner: { value: input.learner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("course", accounts.course),
      getAccountMeta("enrollment", accounts.enrollment),
      getAccountMeta("learner", accounts.learner),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getEnrollInstructionDataEncoder().encode(
      args as EnrollInstructionDataArgs,
    ),
    programAddress,
  } as EnrollInstruction<
    TProgramAddress,
    TAccountCourse,
    TAccountEnrollment,
    TAccountLearner,
    TAccountSystemProgram
  >);
}

export type ParsedEnrollInstruction<
  TProgram extends string = typeof ONCHAIN_ACADEMY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    course: TAccountMetas[0];
    enrollment: TAccountMetas[1];
    learner: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: EnrollInstructionData;
};

export function parseEnrollInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedEnrollInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      course: getNextAccount(),
      enrollment: getNextAccount(),
      learner: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getEnrollInstructionDataDecoder().decode(instruction.data),
  };
}
