/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ONCHAIN_ACADEMY_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_ENROLLMENT_DISCRIMINATOR = new Uint8Array([
  236, 137, 133, 253, 91, 138, 217, 91,
]);

export function getCloseEnrollmentDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_ENROLLMENT_DISCRIMINATOR,
  );
}

export type CloseEnrollmentInstruction<
  TProgram extends string = typeof ONCHAIN_ACADEMY_PROGRAM_ADDRESS,
  TAccountCourse extends string | AccountMeta<string> = string,
  TAccountEnrollment extends string | AccountMeta<string> = string,
  TAccountLearner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCourse extends string
        ? ReadonlyAccount<TAccountCourse>
        : TAccountCourse,
      TAccountEnrollment extends string
        ? WritableAccount<TAccountEnrollment>
        : TAccountEnrollment,
      TAccountLearner extends string
        ? WritableSignerAccount<TAccountLearner> &
            AccountSignerMeta<TAccountLearner>
        : TAccountLearner,
      ...TRemainingAccounts,
    ]
  >;

export type CloseEnrollmentInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseEnrollmentInstructionDataArgs = {};

export function getCloseEnrollmentInstructionDataEncoder(): FixedSizeEncoder<CloseEnrollmentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_ENROLLMENT_DISCRIMINATOR }),
  );
}

export function getCloseEnrollmentInstructionDataDecoder(): FixedSizeDecoder<CloseEnrollmentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseEnrollmentInstructionDataCodec(): FixedSizeCodec<
  CloseEnrollmentInstructionDataArgs,
  CloseEnrollmentInstructionData
> {
  return combineCodec(
    getCloseEnrollmentInstructionDataEncoder(),
    getCloseEnrollmentInstructionDataDecoder(),
  );
}

export type CloseEnrollmentInput<
  TAccountCourse extends string = string,
  TAccountEnrollment extends string = string,
  TAccountLearner extends string = string,
> = {
  course: Address<TAccountCourse>;
  enrollment: Address<TAccountEnrollment>;
  learner: TransactionSigner<TAccountLearner>;
};

export function getCloseEnrollmentInstruction<
  TAccountCourse extends string,
  TAccountEnrollment extends string,
  TAccountLearner extends string,
  TProgramAddress extends Address = typeof ONCHAIN_ACADEMY_PROGRAM_ADDRESS,
>(
  input: CloseEnrollmentInput<
    TAccountCourse,
    TAccountEnrollment,
    TAccountLearner
  >,
  config?: { programAddress?: TProgramAddress },
): CloseEnrollmentInstruction<
  TProgramAddress,
  TAccountCourse,
  TAccountEnrollment,
  TAccountLearner
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ONCHAIN_ACADEMY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    course: { value: input.course ?? null, isWritable: false },
    enrollment: { value: input.enrollment ?? null, isWritable: true },
    learner: { value: input.learner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("course", accounts.course),
      getAccountMeta("enrollment", accounts.enrollment),
      getAccountMeta("learner", accounts.learner),
    ],
    data: getCloseEnrollmentInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseEnrollmentInstruction<
    TProgramAddress,
    TAccountCourse,
    TAccountEnrollment,
    TAccountLearner
  >);
}

export type ParsedCloseEnrollmentInstruction<
  TProgram extends string = typeof ONCHAIN_ACADEMY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    course: TAccountMetas[0];
    enrollment: TAccountMetas[1];
    learner: TAccountMetas[2];
  };
  data: CloseEnrollmentInstructionData;
};

export function parseCloseEnrollmentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseEnrollmentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      course: getNextAccount(),
      enrollment: getNextAccount(),
      learner: getNextAccount(),
    },
    data: getCloseEnrollmentInstructionDataDecoder().decode(instruction.data),
  };
}
