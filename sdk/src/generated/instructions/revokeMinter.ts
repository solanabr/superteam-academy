/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ONCHAIN_ACADEMY_PROGRAM_ADDRESS } from "../programs";

export const REVOKE_MINTER_DISCRIMINATOR = new Uint8Array([
  33, 91, 131, 167, 62, 37, 38, 105,
]);

export function getRevokeMinterDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVOKE_MINTER_DISCRIMINATOR,
  );
}

export type RevokeMinterInstruction<
  TProgram extends string = typeof ONCHAIN_ACADEMY_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountMinterRole extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountMinterRole extends string
        ? WritableAccount<TAccountMinterRole>
        : TAccountMinterRole,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type RevokeMinterInstructionData = { discriminator: ReadonlyUint8Array };

export type RevokeMinterInstructionDataArgs = {};

export function getRevokeMinterInstructionDataEncoder(): FixedSizeEncoder<RevokeMinterInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REVOKE_MINTER_DISCRIMINATOR }),
  );
}

export function getRevokeMinterInstructionDataDecoder(): FixedSizeDecoder<RevokeMinterInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRevokeMinterInstructionDataCodec(): FixedSizeCodec<
  RevokeMinterInstructionDataArgs,
  RevokeMinterInstructionData
> {
  return combineCodec(
    getRevokeMinterInstructionDataEncoder(),
    getRevokeMinterInstructionDataDecoder(),
  );
}

export type RevokeMinterAsyncInput<
  TAccountConfig extends string = string,
  TAccountMinterRole extends string = string,
  TAccountAuthority extends string = string,
> = {
  config?: Address<TAccountConfig>;
  minterRole: Address<TAccountMinterRole>;
  authority: TransactionSigner<TAccountAuthority>;
};

export async function getRevokeMinterInstructionAsync<
  TAccountConfig extends string,
  TAccountMinterRole extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof ONCHAIN_ACADEMY_PROGRAM_ADDRESS,
>(
  input: RevokeMinterAsyncInput<
    TAccountConfig,
    TAccountMinterRole,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RevokeMinterInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountMinterRole,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ONCHAIN_ACADEMY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    minterRole: { value: input.minterRole ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("minterRole", accounts.minterRole),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getRevokeMinterInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeMinterInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountMinterRole,
    TAccountAuthority
  >);
}

export type RevokeMinterInput<
  TAccountConfig extends string = string,
  TAccountMinterRole extends string = string,
  TAccountAuthority extends string = string,
> = {
  config: Address<TAccountConfig>;
  minterRole: Address<TAccountMinterRole>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getRevokeMinterInstruction<
  TAccountConfig extends string,
  TAccountMinterRole extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof ONCHAIN_ACADEMY_PROGRAM_ADDRESS,
>(
  input: RevokeMinterInput<
    TAccountConfig,
    TAccountMinterRole,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): RevokeMinterInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountMinterRole,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ONCHAIN_ACADEMY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    minterRole: { value: input.minterRole ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("minterRole", accounts.minterRole),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getRevokeMinterInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeMinterInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountMinterRole,
    TAccountAuthority
  >);
}

export type ParsedRevokeMinterInstruction<
  TProgram extends string = typeof ONCHAIN_ACADEMY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    minterRole: TAccountMetas[1];
    authority: TAccountMetas[2];
  };
  data: RevokeMinterInstructionData;
};

export function parseRevokeMinterInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevokeMinterInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      minterRole: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getRevokeMinterInstructionDataDecoder().decode(instruction.data),
  };
}
