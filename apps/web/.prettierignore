# lint-staged runs the apps/web config with this directory as its cwd, so
# prettier reads THIS file, not the repo-root .prettierignore. Keep the
# apps/web-relative entries below in sync with the root .prettierignore.

# Generated by `pnpm --filter web compile-content` (scripts/compile-content.ts).
# Byte-stable JSON that CI `git diff --exit-code`s — prettier must not reformat it.
src/content/generated/

# Golden snapshot fixtures for project.golden.test.ts — deep-compared as parsed
# objects, so formatting is irrelevant, and the 580KB lessons.json OOM-kills
# prettier when lint-staged runs it at commit time.
src/lib/content/__tests__/fixtures/golden/
