.next/
.next/**
node_modules
dist
build
.turbo
pnpm-lock.yaml

# Generated by @superteam-lms/content-schema `schema:generate` (z.toJSONSchema).
# The generator is the source of truth; prettier must not reformat its output,
# or `schema:generate` produces a spurious diff.
packages/content-schema/schema/

# Generated by `pnpm --filter web compile-content` (scripts/compile-content.ts).
# The compiler emits byte-stable JSON (JSON.stringify, sorted keys) and CI
# `git diff --exit-code`s it; prettier must not reformat it (it collapses short
# arrays onto one line), or the committed bytes drift from the compiler output.
apps/web/src/content/generated/

# Golden snapshot fixtures for project.golden.test.ts — deep-compared as parsed
# objects, so formatting is irrelevant, and the 580KB lessons.json OOM-kills
# prettier when lint-staged runs it at commit time.
apps/web/src/lib/content/__tests__/fixtures/golden/
