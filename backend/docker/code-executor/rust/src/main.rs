// Pre-built stub; worker overwrites this file with user code.
// Dependencies are already compiled — only this file recompiles per run.
use solana_sdk::pubkey::Pubkey;

fn main() {
    let _ = Pubkey::new_unique();
    println!("Hello, Solana!");
}
