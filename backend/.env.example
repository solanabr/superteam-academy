SOLANA_RPC=https://api.devnet.solana.com
ACADEMY_AUTHORITY_KEYPAIR=
ACADEMY_BACKEND_SIGNER_KEYPAIR=
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
ADMIN_SECRET=
ADMIN_PASSWORD=
BACKEND_API_TOKEN=

# Max JSON body size in bytes (default: 65536). Used by body-guard middleware.
# MAX_BODY_BYTES=65536
# RPC timeout (ms) and max retries for non-transaction reads (defaults: 15000, 2).
# RPC_TIMEOUT_MS=15000
# RPC_MAX_RETRIES=2