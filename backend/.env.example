# Solana RPC (public endpoints ok for devnet)
SOLANA_RPC=https://api.devnet.solana.com

# Required API token for /academy/* routes.
# Send as either:
#   - Authorization: Bearer <token>
#   - X-API-Key: <token>
BACKEND_API_TOKEN=change-me

# Comma-separated allowlist for browser origins.
# Example: http://localhost:3000,https://academy.example.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Authority keypair (for create-course). JSON array of 64 numbers.
ACADEMY_AUTHORITY_KEYPAIR=

# Backend signer keypair (for complete-lesson, finalize-course). JSON array of 64 numbers.
ACADEMY_BACKEND_SIGNER_KEYPAIR=
